{
  "name": "tailwind01",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss build source/style.css -o dist/style.css",
    "watch:tailwind": "nodemon --watch src/style.css -x \"npm run build:css\"",
    "serve": "npm run watch:tailwind",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.4",
    "tailwindcss": "^1.8.12"
  }
}
